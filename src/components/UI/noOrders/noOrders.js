import React from "react";
import classes from "./noOrders.css";

const noOrders = () => {
  return (
    <div>
      <h1 className={classes.text}>No Orders found!</h1>
    </div>
  );
};

export default noOrders;
